apiVersion: v1
kind: Service
metadata:
  name: payment-service
  namespace: order-platform

spec:
  type: ClusterIP

  selector:
    app: payment-service

  ports:
    - protocol: TCP
      port: 8084
      targetPort: 8084